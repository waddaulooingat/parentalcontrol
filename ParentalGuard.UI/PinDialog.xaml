<Window x:Class="ParentalGuard.UI.PinDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Parental Guard" Height="180" Width="340"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="MessageText" Grid.Row="0" Text="Enter parent PIN" FontSize="14" Margin="0,0,0,12" />

        <PasswordBox x:Name="PinBox" Grid.Row="1" FontSize="18" Padding="6"
                     PasswordChar="•" MaxLength="12" />

        <TextBlock x:Name="ErrorText" Grid.Row="2" Foreground="Red" Margin="0,8,0,0"
                   Visibility="Collapsed" TextWrapping="Wrap" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel" Width="80" Margin="0,0,8,0" Click="Cancel_Click" IsCancel="True" />
            <Button Content="OK" Width="80" Click="Ok_Click" IsDefault="True" />
        </StackPanel>
    </Grid>
</Window>
